[pytest]
testpaths = tests
addopts = 
    -v
    --tb=short
    --strict-markers
    -x
    --cov=router
    --cov-report=term-missing
    --cov-report=html
    --cov-report=xml
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
